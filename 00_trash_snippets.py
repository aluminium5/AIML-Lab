import pandas as pd
import numpy as np

# This snippet seems to be a broken fragment in the original file, likely intended to work with 'Final_data.csv' but
# referencing variables from the previous Social Network Ads block.
# Preserved here as 'trash' or 'draft'.

# df = pd.read_csv('Final_data.csv')
# display(df.head())
# from sklearn import preprocessing#(18-60)&(15k-1.5L)
# from sklearn.preprocessing import MinMaxScaler
# scalar=MinMaxScaler()
# scalar.fit(X_train_imputed)
# X_train_scaled=scalar.transform(X_train_imputed)
# X_test_scaled=scalar.transform(X_test_imputed)
# X_train_scaled

# #Use feature Scaling to convert different scales to a standard scale to make it easier for Machine learning algorithms
# from sklearn import preprocessing#(18-60)&(15k-1.5L)
# from sklearn.preprocessing import MinMaxScaler
# scalar=MinMaxScaler()
# scalar.fit(X_train_imputed)
# X_train_scaled=scalar.transform(X_train_imputed)
# X_test_scaled=scalar.transform(X_test_imputed)
# X_train_scaled

# from sklearn.naive_bayes import GaussianNB
# model = GaussianNB()
# model.fit(X_train_scaled, y_train)
